kind: pipeline
type: docker
name: default

steps:
  - name: deploy
    image: docker/compose
    commands:
      - /bin/bash ./.drone/deploy.sh
      - sh ./.drone/deploy.sh
    volumes:
      - name: docker_socket
        path: /var/run/docker.sock

volumes:
  - name: docker_socket
    host:
      path: /var/run/docker.sock
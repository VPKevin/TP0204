kind: pipeline
type: docker
name: default

steps:
  - name: deploy
    image: docker/compose:debian-1.29.2
    commands:
#      - ls /bin -la --color
#      - bash ./.drone/deploy.sh
      - bash ./.drone/deploy-bash.sh
#      - sh ./.drone/test.sh
    volumes:
      - name: docker_socket
        path: /var/run/docker.sock

volumes:
  - name: docker_socket
    host:
      path: /var/run/docker.sock
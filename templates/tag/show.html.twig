{% extends 'base.html.twig' %}

{% block title %}Catégorie - {{ tag.label }}{% endblock %}

{% block body %}
    <h1>Catégorie :</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ tag.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tag_index') }}">Retour à la liste</a>

    <a href="{{ path('tag_edit', {'id': tag.id}) }}">Modifier</a>

    {{ include('tag/_delete_form.html.twig') }}
{% endblock %}
